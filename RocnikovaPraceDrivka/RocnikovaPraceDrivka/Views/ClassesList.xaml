<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:myE="clr-namespace:RocnikovaPraceDrivka.MyElements"
			 x:Class="RocnikovaPraceDrivka.Views.ClassesList"
			 Title="Classes list"
			 NavigationPage.HasNavigationBar="False"
			 NavigationPage.HasBackButton="False"
			 NavigationPage.BackButtonTitle="False">

	<StackLayout x:Name="AllStuff">

		<CollectionView x:Name="list"
						BackgroundColor="Black">

			<CollectionView.ItemsLayout>
				<LinearItemsLayout 
					Orientation="Vertical"
					ItemSpacing="10"/>
			</CollectionView.ItemsLayout>

			<CollectionView.Header>
				<Frame Margin="0,20,0,20" 
						   Padding="8"
						   CornerRadius="5"
						   BackgroundColor="LightGray">
					<Label Text="List of your classes"
							   TextColor="White"/>
				</Frame>
			</CollectionView.Header>

			<CollectionView.EmptyView>
				<Frame>
					<Label Text="No classes created yet"
						   HorizontalOptions="Center" 
						   VerticalOptions="Center"
						   TextColor="White"/>
				</Frame>
			</CollectionView.EmptyView>

			<CollectionView.ItemTemplate>
				<DataTemplate>
					<Frame HeightRequest="50"
						   CornerRadius="5"
						   BackgroundColor="LightGray">
						<Grid>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="*"/>
								<ColumnDefinition Width="2*"/>
								<ColumnDefinition Width="*"/>
							</Grid.ColumnDefinitions>


							<Label Text="{Binding Name}"
								   TextColor="White"
								   Grid.Column="0"
								   VerticalTextAlignment="Center"
								   HorizontalTextAlignment="Center"/>

							<Label Text="{Binding Desc}"
								   TextColor="White"
								   Grid.Column="1"
								   HorizontalTextAlignment="Center"
								   VerticalTextAlignment="Center"/>


							<myE:MyClassButton Text="Details"
											   Grid.Column="2"
											   Clicked="DetailsButton_Clicked"/>

						</Grid>
					</Frame>
				</DataTemplate>
			</CollectionView.ItemTemplate>




		</CollectionView>
	</StackLayout>
</ContentPage>